<Page x:Class="CmdManager.Views.GroupsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent" Title="Groups">
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Grid Grid.Row="0" Margin="0,0,0,20">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>
      <StackPanel>
        <TextBlock Text="Groups" FontSize="26" FontWeight="Bold" Foreground="{StaticResource TextBrush}"/>
        <TextBlock Text="Organize commands, chains, and registry items into collections." FontSize="13" Foreground="{StaticResource MutedTextBrush}" Margin="0,2,0,0"/>
      </StackPanel>
      <Button Grid.Column="1" Content="+ New Group" Style="{StaticResource PrimaryButton}"
              VerticalAlignment="Center" Click="AddGroup_Click"/>
    </Grid>

    <TextBox x:Name="SearchBox" Grid.Row="1" Style="{StaticResource SearchBox}"
             TextChanged="Search_Changed" Margin="0,0,0,16"/>

    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
      <StackPanel x:Name="GroupList"/>
    </ScrollViewer>
  </Grid>
</Page>
