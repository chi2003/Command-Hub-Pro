<Window x:Class="CmdManager.Views.ChainDetailDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chain Detail" Width="560" Height="480"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}"
        Foreground="{StaticResource TextBrush}">
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock x:Name="TitleText" Grid.Row="0" FontSize="20" FontWeight="Bold"
               Foreground="{StaticResource TextBrush}"/>
    <TextBlock x:Name="DescText" Grid.Row="1" FontSize="13"
               Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,6,0,8"/>

    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
      <StackPanel x:Name="StepList"/>
    </ScrollViewer>

    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
      <Button Content="▶ Run Chain" Style="{StaticResource PrimaryButton}" Click="Run_Click" Margin="0,0,6,0"/>
      <Button Content="✏ Edit" Style="{StaticResource GhostButton}" Click="Edit_Click" Margin="0,0,6,0"/>
      <Button Content="🗑 Delete" Style="{StaticResource DangerButton}" Click="Delete_Click" Margin="0,0,6,0"/>
      <Button Content="Close" Style="{StaticResource GhostButton}" Click="Close_Click"/>
    </StackPanel>
  </Grid>
</Window>
